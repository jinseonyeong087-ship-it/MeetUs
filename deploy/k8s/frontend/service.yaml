apiVersion: v1
kind: Service
metadata:
  name: ai-minutes-frontend-svc
  labels:
    app: ai-minutes-frontend
spec:
  type: ClusterIP
  selector:
    app: ai-minutes-frontend
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
